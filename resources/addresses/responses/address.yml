description: Returns an address object if a valid identifier was provided.

headers:
  ratelimit-limit:
    $ref: '../../../shared/headers/rate-limiters.yml#/ratelimit-limit'
  ratelimit-remaining:
    $ref: '../../../shared/headers/rate-limiters.yml#/ratelimit-remaining'
  ratelimit-reset:
    $ref: '../../../shared/headers/rate-limiters.yml#/ratelimit-reset'

content:
  application/json:
    schema:
      $ref: '../models/address.yml'