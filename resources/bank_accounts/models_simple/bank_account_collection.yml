type: object
properties:
  object:
    $ref: "../../../shared/attributes/object.yml"

  next_url:
    type: string
    description: Url of next page of items in list.
    nullable: true

  previous_url:
    type: string
    description: Url of previous page of items in list.
    nullable: true

  count:
    $ref: "../../../shared/attributes/count.yml"

  data:
    type: array
    description: list of bank_accounts
    items:
      $ref: "../models_simple/bank_account.yml"