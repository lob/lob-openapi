allOf:
  - $ref: "billing_group_base.yml"

    required:
      - name

    properties:
      date_created:
        description: A timestamp in ISO 8601 format of the date the billing group was created.
        type: timestamp

      date_modified:
        description: A timestamp in ISO 8601 format of the date the billing group was created.
        type: timestamp
