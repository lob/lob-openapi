allOf:
  - $ref: "../../../shared/models/lob_base.yml"
  - $ref: "campaign_writable.yml"

  - type: object

    required:
      - id
      - name
      - description
      - schedule_type
      - use_type
      - is_draft
      - creatives
      - uploads
      - auto_cancel_if_ncoa
      - date_created
      - date_modified
      - object

    properties:
      id:
        $ref: "../attributes/cmp_id.yml"

      is_draft:
        description: Whether or not the campaign is still a draft.
        type: boolean
        default: true

      creatives:
        description: An array of creatives that have been associated with this campaign.
        type: array

      uploads:
        description: A single-element array containing an existing upload id in a string format.
        type: array
        items:
          $ref: "../../uploads/attributes/upl_id.yml"
        minItems: 0
        maxItems: 1
        nullable: true

      object:
        type: string
        description: Value is resource type.
        enum:
          - campaign
        default: campaign
