description: Returns a campaign object

headers:
  ratelimit-limit:
    $ref: "../../../shared/headers/ratelimit.yml#/ratelimit-limit"
  ratelimit-remaining:
    $ref: "../../../shared/headers/ratelimit.yml#/ratelimit-remaining"
  ratelimit-reset:
    $ref: "../../../shared/headers/ratelimit.yml#/ratelimit-reset"

content:
  application/json:
    schema:
      $ref: "../models/campaigns.yml"

    example:
      foobar: triangles
