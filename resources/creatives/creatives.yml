post:
  operationId: creative

  summary: Do Thing

  description: >-
    Does something to creative

  tags:
    - Creatives

  parameters:
    - $ref: "../../shared/parameters/lang_spec.yml"

  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "models/creatives.yml"
        example:
          description: demo
          # complete example using all create payload properties

      application/x-www-form-urlencoded:
        schema:
          $ref: "models/creatives.yml"
        encoding: # be sure to check which encodings apply
          example-prop:
            style: deepObject
            explode: true
        example:
          description: encoded demo
          # complete example using all create payload properties
          # matching encoding style

      multipart/form-data:
        schema:
          $ref: "models/creatives.yml"
        example:
          description: demo
          # complete example using all create payload properties

  responses:
    "200":
      $ref: responses/creatives.yml

    default:
      $ref: responses/creatives_error.yml

  x-codeSamples:
    - lang: Shell
      label: CURL
      source: |

    - lang: Javascript
      label: NODE
      source: |

    - lang: Ruby
      label: RUBY
      source: |

    - lang: Python
      label: PYTHON
      source: |

    - lang: PHP
      source: |

    - lang: Java
      label: JAVA
      source: |

    - lang: Elixir
      label: ELIXIR
      source: |
