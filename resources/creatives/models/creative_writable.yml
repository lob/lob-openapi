allOf:
  - $ref: "creative_base.yml"

  - type: object

    required:
      - campaign_id
      - resource_type
      - details

    properties:
      campaign_id:
        $ref: "../../campaigns/attributes/cmp_id.yml"
