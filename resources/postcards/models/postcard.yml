type: object

required:
  - to
  - front
  - back

properties:
  description:
    $ref: "../../../shared/attributes/resource_description.yml"

  to:
    description: >-
      Must be an address ID blah blah
    oneOf:
      - $ref: "../../../shared/models/address_lines_us.yml"
      - $ref: "../../../shared/models/address_lines_intl.yml"
      - $ref: "../../../shared/attributes/address/adr_id.yml"

  from:
    description: >-
      Must be an address ID blah blah
    oneOf:
      - $ref: "../../../shared/models/address_lines_us.yml"
      - $ref: "../../../shared/models/address_lines_intl.yml"
      - $ref: "../../../shared/attributes/address/adr_id.yml"

  send_date:
    $ref: "../../../shared/attributes/send_date.yml"

  front:
    $ref:
      oneOf:
        - "../attributes/artwork.yml"
        - "../../../shared/attributes/tmpl_id.yml"

  back:
    $ref:
      oneOf:
        - "../attributes/artwork.yml"
        - "../../../shared/attributes/tmpl_id.yml"

  size:
    $ref: "../../../shared/attributes/tmpl_id.yml"

  mail_type:
    $ref: "../../../shared/attributes/mail_type.yml"

  merge_variables:
    $ref: "../../../shared/models/merge_variables.yml"

  metadata:
    $ref: "../../../shared/models/metadata.yml"
