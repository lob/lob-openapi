title: Upload State
enum:
  - Preprocessing
  - Draft
  - Ready for Validation
  - Validating
  - Scheduled
  - Cancelled
  - Errored
default: Draft
type: string
description: "`state` property on the `upload` object"
