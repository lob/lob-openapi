description: >-
  An array of matching uploads. Each entry in the array is a separate
  upload.

content:
  application/json:
    schema:
      type: array
      items:
        $ref: "../models/upload.yml"
    example:
