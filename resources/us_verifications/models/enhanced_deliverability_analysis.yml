type: object

required:
  - lob_footnotes

properties:
  lob_footnotes:
    type: array
    description: >
      An array of 1-character and 2-character strings that gives more insight into how `enhanced_deliverability` was determined.
      Will always include 2 strings For details, see [US Verification Details](#tag/US-Verification-Details).
    items:
      $ref: "../attributes/lob_footnote.yml"
    example: [H]
