type: object

required:
  - address

properties:
  address:
    type: string
    description: >
      The entire address in one string. Does not support a recipient and will
      error when other payload parameters are provided.
    example: "185 Berry Street 94107"

  object:
    $ref: "../../../shared/attributes/object.yml"
