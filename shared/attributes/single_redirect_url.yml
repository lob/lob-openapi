type: string
description: Redirect all mail receipients to a single non-personalized web page. To use this type of redirection, just add a redirect_url with the url where the users should be redirected to after scanning the QR code.
