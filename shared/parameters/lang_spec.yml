in: header

name: x-lang-output

required: false

description: >
  Translate response to the native language of the country in the 
  request `native` or match the response to the language in the 
  request `match`. Default response is in English.

schema:
  type: string
  enum: [native, match]
