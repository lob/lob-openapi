in: query

name: sort_by

description: >
  Sorts billing_group in a desired order.
  `sort_by` accepts an object with the key being either `date_created` or `date_modified`
  and the value being either `asc` or `desc`.

schema:
  type: string
