description: Returns true if the delete was successful

content:
  application/json:
    schema:
      type: object
      properties:
        id:
          schema:
            $ref: ../resources/addresses/attributes/adr_id.yml
        message:
          type: string
          value: "deleted: true"
