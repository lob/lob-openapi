description: Returns true if the delete was successful

content:
  application/json:
    schema:
      type: object
      properties:
        id:
          $ref: "../attributes/address/adr_id.yml"
        deleted:
          $ref: "../attributes/deleted.yml"
